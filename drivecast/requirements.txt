fastapi
uvicorn
httpx
qrcode         # SVG QR codes for the "Watch on iPhone / iPad" remote-access URL
rumps          # menu-bar app (drivecast_menubar.py / drivecast.app)

# optional:
#   h2           enables HTTP/2 for the streaming proxy (faster keep-alive);
#                drivecast falls back to HTTP/1.1 automatically if it's absent.

# dev extras:
#   pytest
#   py2app       (build-only; see requirements-dev.txt and the README build section)
