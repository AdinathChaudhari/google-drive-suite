[
  {
    "_comment": "NOTE: if this toolkit lives inside a google-drive-suite checkout (this file sitting at .../google-drive-suite/toolkit/), drivecast / drive-offload / drivecast-app below are already registered as BUILT-INS by hub/registry.py — you do not need these entries at all in that case. This example is for the STANDALONE toolkit install (no suite checkout), where these tools live in their own sibling repos and need to be registered by hand.",
    "id": "drivecast",
    "label": "Drivecast",
    "kind": "web",
    "dir": "~/Documents/Playground/GitHub/drivecast",
    "installed_if": ["app.py", "venv/bin/python"],
    "launch": { "argv": ["venv/bin/python", "app.py"] },
    "port": {
      "config": "~/Library/Application Support/drivecast/config.json",
      "key": "port",
      "default": 8737
    }
  },
  {
    "id": "drive-offload",
    "label": "Drive Offload",
    "kind": "menubar",
    "dir": "~/Documents/Playground/GitHub/drive-offload",
    "installed_if_any": [
      "~/Library/LaunchAgents/com.driveoffload.app.plist",
      "/Applications/drive-offload.app"
    ],
    "launchd_label": "com.driveoffload.app",
    "process_pattern": "drive-offload\\.app/Contents/MacOS|offload_app\\.py"
  },
  {
    "id": "drivecast-app",
    "label": "Drivecast (Fire TV)",
    "kind": "external",
    "note": "Android TV / Fire TV client, not a process this Mac can launch or check the status of. Listed here as a reminder of what exists in the ecosystem; the hub should render an 'external' kind as an inert, non-clickable row rather than erroring on the missing launch/port fields other kinds require."
  },
  {
    "_comment": "Any entry above can still be used INSIDE a suite checkout too — e.g. to override the drivecast built-in's port or dir. A user id that matches a built-in id REPLACES it rather than duplicating the row.",
    "id": "some-other-tool",
    "label": "Some Other Tool",
    "kind": "web",
    "dir": "~/Documents/Playground/GitHub/some-other-tool",
    "installed_if": ["app.py"],
    "launch": { "argv": ["venv/bin/python", "app.py"] },
    "port": { "config": "~/Library/Application Support/some-other-tool/config.json", "key": "port", "default": 9000 }
  }
]
